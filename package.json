{
  "name": "my-render-bot",
  "version": "1.0.0",
  "description": "A simple bot hosted on Render",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "discord.js": "^14.0.0" 
    // If your code uses other libraries (like axios or dotenv), 
    // you MUST list them here in the format: "library-name": "^version"
  }
}
